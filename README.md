# Andrei-Cristian Cojocaru

Source for [andreiberkeley.github.io](https://andreiberkeley.github.io), a research-focused academic profile.

The site is a static HTML and CSS page hosted with GitHub Pages. Edit `index.html` for content and `style.css` for presentation; no build step is required.

The layout adapts to smaller screens, follows the system's light/dark preference automatically, and uses a light print stylesheet.

`assets/Andrei_Cojocaru_CV.pdf` is the public CV. Keep it separate from private application and recommendation materials. Current research is described as ongoing. The public CV omits the phone number.

The CS180 portfolio is maintained separately at [andreiberkeley.github.io/cs180/](https://andreiberkeley.github.io/cs180/).

September 10 final review: the local page includes the verified Scholar profile, clarified ongoing research, corrected experience descriptions, and simulation/world models as interests only. Publication metadata was checked against the linked papers and GEPA article. The retained Project 0 page's portfolio links now point to the separate CS180 site.

September 11 publication package: the public CV matches the reviewed two-page version in `CV/v2/public/`, including the expanded ML/NLP, library, cloud, and development-tool skills. The publication also includes the revised research descriptions, Scholar link, compact layout, automatic light/dark styles, and corrected CS180 back-links. GitHub Pages deploys the root of this repository's `main` branch.

LinkedIn blocked automated checks; the supplied profile URL has been retained rather than treated as broken. Static asset, mobile/dark stylesheet, keyboard-focus and contrast checks passed; this pass did not include interactive browser testing.
